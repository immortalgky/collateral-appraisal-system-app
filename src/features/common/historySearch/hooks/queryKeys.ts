/**
 * Query key factory for History Search feature.
 * Centralized to avoid inline string arrays scattered across hooks.
 */
export const historySearchKeys = {
  // No query-based keys remain — the history search uses a mutation (POST).
  // Keys are preserved here as a factory stub for future query additions.
} as const;
