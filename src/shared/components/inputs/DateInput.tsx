/**
 * DateInput - Date picker with calendar popup
 *
 * This component wraps DatePickerInput to provide a calendar-based date selection
 * instead of the native HTML5 date input.
 */
import DatePickerInput from './DatePickerInput';

export default DatePickerInput;
