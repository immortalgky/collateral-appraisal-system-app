// Superseded by ProvinceAutocomplete (typeahead). This file is kept to prevent
// import errors if referenced outside src/. Nothing in src/ imports it.
export { default } from './ProvinceAutocomplete';
